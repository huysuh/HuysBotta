todo list

hard bots:
  add a 'armor' command that toggles bots taking off their armor or not
  add a 'drop' command to make bots drop everything in their inventory
  auto leave when more than threshold joins the lobby?

silent bots:
  look at players and attack
  swap lobby until found player
  
  
